# Clip9 — App Store Listing Kit

This document contains copy and assets for App Store Connect.

---

## App Name

Clip9

## Subtitle (30 characters max)

Full-Fidelity Copy History

## Promotional Text (170 characters max)

Every copy captured in full — text, images, GIFs, videos, files, rich text — restored exactly as copied. Smart deduplication, zero network access. Your clipboard, owned.

## Description

You copy things constantly. Text from a document, a screenshot, a block of code with syntax highlighting, a set of files from Finder. Five minutes later you need it again — but you've already copied something else. It's gone.

Clip9 brings it back.

Clip9 lives in your menu bar and silently captures every copy in full fidelity — every data type, every representation — so you can restore any item exactly as it was. The receiving app sees no difference from the original. Text formatting, image data, file references, rich text with syntax highlighting — all intact.

FULL-FIDELITY CAPTURE
Clip9 captures every UTI type on the macOS pasteboard: plain text, rich text, HTML, images, animated GIFs, videos, audio, files, and arbitrary binary data. Multi-item clipboard copies from Finder are captured together. When you restore an entry, it's byte-for-byte identical to the original.

RICH PREVIEWS
See your clipboard history at a glance. Animated GIFs play inline. Videos loop silently. Images show fitted thumbnails. Rich text from IDEs and editors preserves its original syntax highlighting, font styles, and background colors. Files display their name and count. Sensitive items from password managers show a privacy placeholder — the content is never stored.

MENU BAR NATIVE
Clip9 lives entirely in your menu bar — no Dock icon, no window clutter. Click to open a floating, translucent frosted-glass panel anchored below the icon. Click outside or press Escape to dismiss. Right-click for Settings, Clear History, and more.

FLUID NAVIGATION
Mouse hover highlights cards in real time. Arrow keys move the selection. A chevron appears when more content is below — hover over it and the list scrolls smoothly at a constant speed. Mouse and keyboard work together seamlessly. Click or press Enter to restore.

ADJUSTABLE ZOOM
Resize cards with Cmd+ and Cmd- (0.5x to 3.0x), or use the slider in Settings. The panel resizes dynamically, growing leftward from the menu bar icon. Cmd+0 resets to default.

SMART DEDUPLICATION
Copy the same thing twice? Clip9 promotes the existing entry instead of cluttering your history. When an app writes the same data plus additional types, the existing entry is upgraded in place. Clipboard changes made by Clip9 itself are automatically ignored.

PERSISTENT AND CONFIGURABLE
Your clipboard history survives app restarts and system reboots. Configure max history (10–500 items), storage cap (0.1–10 GB), and launch at login — all from a clean Settings panel.

PRIVATE BY DESIGN
Clip9 makes zero network requests. No analytics. No telemetry. No cloud sync. No account required. No model downloads. No update checks. Your clipboard data never leaves your Mac. Period.

BUILT FOR YOUR MAC
Clip9 is a native Swift and SwiftUI application — not an Electron wrapper, not a web app in disguise. It uses AppKit for its floating panel, native scroll physics, and real-time mouse tracking. It looks, feels, and performs like a Mac app should.

---

What's in the box:
- Full-fidelity clipboard capture for every macOS data type
- Rich previews — animated GIFs, video playback, syntax-highlighted text, image thumbnails
- Menu bar native — floating translucent panel, no Dock icon
- Fluid navigation — mouse hover, keyboard arrows, hover-scroll zone
- Adjustable zoom — Cmd+/Cmd- or Settings slider (0.5x to 3.0x)
- Smart deduplication and superset coalescing
- Concealed content detection — password managers excluded automatically
- Persistent storage — survives restarts and reboots
- Configurable history size, storage cap, and launch at login
- Zero network access — fully offline, fully private
- Native macOS experience with keyboard shortcuts

## Keywords (100 characters max)

clipboard,history,paste,copy,manager,pasteboard,rich text,images,privacy,menu bar

## What's New (for initial release)

Initial release — Clip9 captures your complete clipboard history with full fidelity and restores every item exactly as it was. Text, images, GIFs, videos, files, rich text — all preserved.

## Category

Utilities

## Secondary Category

Productivity

---

## Pricing

One-time purchase: $7.99

---

## Screenshot Text Overlays

Screenshot 1 — Hero

Your clipboard history. Full fidelity. Always there.

Screenshot 2 — Rich Previews

Images, GIFs, videos, rich text — all previewed inline.

Screenshot 3 — Menu Bar

Lives in your menu bar. Click to open. Click outside to dismiss.

Screenshot 4 — Navigation

Hover, scroll, arrow keys. Fluid and fast.

Screenshot 5 — Privacy

Zero network requests. Your clipboard never leaves your Mac.

---

## Release Notes Template

Version 1.0

Initial release.

Clip9 captures your complete clipboard history with full fidelity and restores every item exactly as it was.

Features:

- Full-fidelity capture for every macOS clipboard data type
- Rich previews with animated GIF playback and video loops
- Rich text preservation — syntax highlighting, font styles, and colors intact
- Menu bar native with floating translucent panel
- Fluid navigation — mouse hover, keyboard arrows, hover-scroll zone
- Adjustable zoom (Cmd+/Cmd-, 0.5x to 3.0x)
- Smart deduplication and superset coalescing
- Concealed content detection for password managers
- Configurable history size, storage cap, and launch at login
- Private by design — zero network requests

---

## App Store Review Notes

Clip9 is a clipboard history manager that monitors the macOS system pasteboard and stores copies locally for later retrieval. No special permissions are required.

The app makes zero network requests. There is no login, no account creation, and no server communication of any kind. All data is stored locally in the app's Application Support directory within the macOS sandbox.

Sensitive clipboard content marked as concealed (e.g., from password managers using org.nspasteboard.ConcealedType) is detected and excluded from storage automatically.

---

## Hacker News Launch Post

Title:

Show HN: Clip9 — Full-fidelity clipboard history for macOS

Post:

I built a macOS clipboard history manager that captures every data type on the pasteboard and restores items with byte-for-byte accuracy.

Most clipboard managers lose fidelity — they capture the plain text but drop the rich text formatting, or they save images but lose the original UTI types. When you paste, the receiving app gets a degraded version. Clip9 captures everything: every UTI type, every data representation, exactly as the source app provided it.

It lives in the menu bar as a floating, translucent panel. Animated GIFs play inline. Videos loop silently. Rich text from IDEs keeps its syntax highlighting. Files show their name and count.

Navigation is designed to feel native — mouse hover highlights cards, arrow keys move selection, and a hover-scroll zone at the bottom provides smooth constant-speed scrolling. Cmd+/- adjusts the card size from 0.5x to 3.0x.

Smart deduplication detects when you copy the same thing twice and promotes the existing entry. If an app writes the same data plus additional types, the entry is upgraded in place rather than duplicated.

Zero network requests. No analytics. No telemetry. No cloud. Everything local.

Would love feedback.

---

## Product Hunt Listing Copy

Title

Clip9

Tagline

Full-fidelity clipboard history for macOS. Every copy preserved exactly.

Description

Clip9 captures every copy you make — text, images, animated GIFs, videos, files, rich text with syntax highlighting — and stores it with complete fidelity. Restore any item and the receiving app sees no difference from the original.

Key features:

- Full-fidelity capture for every macOS data type
- Rich previews with animated playback and syntax highlighting
- Menu bar native — floating translucent panel
- Smart deduplication and superset coalescing
- Adjustable zoom (0.5x to 3.0x)
- Zero network requests — fully private

Your clipboard data never leaves your Mac.

---

## Demo Video Outline

20-30 seconds.

Scene 1 — Copy text from a document. Show it appear in Clip9's panel.

Scene 2 — Copy an animated GIF. Show it playing inline in the history.

Scene 3 — Copy syntax-highlighted code from an IDE. Show rich text preserved in the card.

Scene 4 — Copy multiple files from Finder. Show the grouped file entry.

Scene 5 — Scroll through history, hover to highlight, click to restore.

Scene 6 — Cmd+ to zoom in, Cmd- to zoom out.

Final screen: Your clipboard history. Full fidelity. Always there.
